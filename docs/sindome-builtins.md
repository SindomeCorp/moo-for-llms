# Sindome-Specific Builtins

These builtins are Sindome-specific and should make an example
`dialect: core-specific`, not `dialect: toaststunt`.

```text
sql_query
sql_connections
sql_open
sql_close
sql_info
```
