# Patch-Specific Builtins

These builtins come from public or semi-public server patches that are not part
of stock LambdaMOO or ToastStunt. They should make an example
`dialect: patch-specific`, not `dialect: portable`, `toaststunt`, or
`core-specific`.

```text
xml_parse_tree
xml_parse_document
```
